import 'package:flutter/material.dart';

class SyncConfirmation extends StatelessWidget {
  const SyncConfirmation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
